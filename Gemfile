source "https://rubygems.org"

# Matches the exact Jekyll environment GitHub Pages builds with,
# so a local preview behaves the same as production.
gem "github-pages", group: :jekyll_plugins

# Required for `jekyll serve` on Ruby 3.0+
gem "webrick", "~> 1.8"
